{
    "version": 2,
    "buildCommand": "yarn build",
    "outputDirectory": ".",
    "devCommand": "yarn dev",
    "routes": [
        {
          "src": "/(.*)",
          "dest": "src/main.ts"
        }
      ]
}
