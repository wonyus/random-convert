{
    "version": 2,
    "buildCommand": "yarn build",
    "outputDirectory": ".",
    "devCommand": "yarn dev",
    "routes": [
        {
          "src": "/(.*)",
          "dest": "dist/main.js"
        }
      ]
}
